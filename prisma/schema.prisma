generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Assessment {
  id                    String    @id @default(cuid())
  name                  String
  position              String
  phone                 String
  email                 String
  establishmentType     String
  bedCount              String
  cmeProfessionals      String
  region                String
  state                 String
  consentGiven          Boolean
  totalScore            Float
  managementScore       Float?
  processScore          Float?
  technologyScore       Float?
  financialScore        Float?
  lgpdScore             Float?
  createdAt             DateTime  @default(now())
  responses             String    @db.Text
  status                String    @default("pending")
  adminObservation      String?   @db.Text
  economyMinEdited      Float?
  economyMaxEdited      Float?
  financialRiskLevelEdited  String?
  financialLossEdited   String?
  releasedAt            DateTime?
  resultJson            String?   @db.Text
}
